# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# crawler raw snapshots (uploaded as CI artifacts instead)
.crawl-raw/

# KCIA 첨부 자동 다운로드 (매 cron 마다 새로 받음. parser 가 즉시 사용 후
# regulations.json 만 commit. raw 첨부 파일 자체는 git 비대 방지로 제외)
public/data/raw-attach/

# typescript
*.tsbuildinfo
next-env.d.ts

# Local Netlify folder
.netlify

# e2e
.e2e-shots/

# public/data/*.json 는 git 에 포함 — 사용자가 git clone + npm run build 만으로
# Supabase 자격증명 없이 단독 구동 가능하도록.

# Portable Node.js (start.bat 가 자동 다운로드 — git 무관)
node-portable/
node-portable.zip
_sweepfull.log
